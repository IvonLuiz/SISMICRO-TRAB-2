#include <xc.h>
#include <stdio.h>
#include <stdlib.h>

#include "USART.h"

int main(void) {
    // Inicinado o USART.
    // OBS: Para todos os comandos, verificar arquivo "USART.h"
    USART_Init(MYUBRR);


    while (1) {
    }

    return 0;
}